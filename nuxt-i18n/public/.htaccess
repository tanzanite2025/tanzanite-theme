<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /widget/
  
  # Redirect all requests to index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /widget/index.html [L]
</IfModule>
