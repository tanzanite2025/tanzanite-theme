# 🚀 快速启动指南

## ⏳ 当前状态
正在安装 `@nuxtjs/i18n` 依赖...

## 📝 等待安装完成后的步骤

### 1. 验证安装
```bash
cd nuxt-i18n-widget
npm list @nuxtjs/i18n
```

如果看到版本号，说明安装成功。

### 2. 启动开发服务器
```bash
npm run dev
```

### 3. 访问测试
打开浏览器访问：
- http://localhost:3000/widget/ （英语）
- http://localhost:3000/widget/fr/ （法语）
- http://localhost:3000/widget/de/ （德语）
- http://localhost:3000/widget/es/ （西班牙语）
- http://localhost:3000/widget/ja/ （日语）
- http://localhost:3000/widget/ko/ （韩语）
- http://localhost:3000/widget/it/ （意大利语）
- http://localhost:3000/widget/pt/ （葡萄牙语）
- http://localhost:3000/widget/ru/ （俄语）
- http://localhost:3000/widget/ar/ （阿拉伯语）

### 4. 测试语言切换器
点击页面右上角的语言切换器，选择不同语言。

### 5. 构建生产版本
```bash
npm run generate
```

生成的文件在 `.output/public/` 目录。

### 6. 部署到 WordPress
将 `.output/public/` 的内容复制到：
```
WordPress根目录/widget/
```

## 🔧 如果安装失败

### 方案 A：清理重装
```bash
rm -rf node_modules package-lock.json
npm install
```

### 方案 B：使用 yarn
```bash
npm install -g yarn
yarn install
yarn dev
```

### 方案 C：检查 Node 版本
```bash
node -v
```
需要 Node.js 18.0.0 或更高版本。

## 📚 完整文档
查看 `README-I18N.md` 了解详细信息。

## ✅ 已完成的工作

- ✅ 创建 10 种语言的翻译文件
- ✅ 配置 Nuxt i18n 模块
- ✅ 创建语言切换器组件
- ✅ 创建示例页面
- ✅ 配置 SEO 优化
- ✅ 配置静态生成

## 🎯 下一步

1. 等待 npm install 完成
2. 运行 `npm run dev` 测试
3. 根据需要修改翻译内容
4. 构建并部署到 WordPress
